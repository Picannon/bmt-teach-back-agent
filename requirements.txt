# Install the latest anthropic SDK — structured outputs (output_config.format)
# and adaptive thinking require a recent version.
anthropic
python-dotenv

# Web app: serves the JSON API + the mobile-styled frontend.
fastapi
uvicorn[standard]

# Live voice agent: WebSocket client to Deepgram's streaming STT (raw WS, no SDK).
websockets