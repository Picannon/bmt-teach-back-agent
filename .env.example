# Copy to .env and fill in. .env is gitignored — never commit a real key.
ANTHROPIC_API_KEY=

# Deepgram streaming STT for the live voice agent (voice_agent.py). Without it,
# the mic path returns an error and the app falls back to the synthetic transcript.
DEEPGRAM_API_KEY=